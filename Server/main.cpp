#include <iostream>

#include "main.h"

int main()
{
    int buffer = 3;
    cout << "Start:\n\n";

    //query_computations(db_pubkey, db_seckey, cypher, bitM, lines, columns);

    return buffer;
}